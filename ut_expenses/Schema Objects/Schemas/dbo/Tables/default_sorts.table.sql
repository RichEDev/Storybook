CREATE TABLE [dbo].[default_sorts] (
    [employeeid]   INT            NOT NULL,
    [gridid]       INT            NOT NULL,
    [columnname]   NVARCHAR (100) NOT NULL,
    [defaultorder] TINYINT        NOT NULL
);

